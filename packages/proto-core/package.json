{
  "name": "@lowcode/proto-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "proto:gen": "bash ./scripts/gen.sh",
    "build": "echo 'build skipped for proto-core (source-of-truth is .proto files)'",
    "typecheck": "echo 'typecheck skipped for proto-core (source-of-truth is .proto files)'",
    "lint": "echo 'lint reserved for proto-core'"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.11.0"
  },
  "devDependencies": {
    "ts-proto": "^2.6.1",
    "typescript": "^5.7.3"
  }
}
